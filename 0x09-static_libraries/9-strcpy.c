#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * *_strcpy - function
 * @dest: string
 * @src: string
 * Return: to dest
 */
char *_strcpy(char *dest, char *src)
{
	return (strcpy(dest, src));
}
