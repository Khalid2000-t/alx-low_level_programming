#include <stdio.h>
/**
 * main - print all single digits base on 10
 * Return: always return 0
 */
int main(void)
{
	int base;

	for (base = 0; base < 10; base++)

		printf("%d", base);
	printf("\n");

	return (0);
}
